{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "_Note": "Kafka:BootstrapServers is overridden by environment variables. When running via scripts/build-and-deploy.sh, values are loaded from backend/realtime-notifications/scripts/.env.",
  "Kafka": {
    "BootstrapServers": "localhost:9092"
  }
}